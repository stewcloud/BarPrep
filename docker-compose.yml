services:
  kitchen-labels:
    build: .
    container_name: kitchen-labels
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8540}:8540"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
